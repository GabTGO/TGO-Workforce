# Pydantic request/response schemas live here, one module per resource
# (e.g. employee.py mirroring app/models/employee.py).
#
# Empty for now — populated once the Postgres table schema is provided.
